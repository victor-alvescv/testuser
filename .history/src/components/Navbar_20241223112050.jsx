import React from 'react';
import logo from '../'
import { Link } from 'react-router-dom';

const Nav = () => {
    return (
        <nav>
          <img src={logo} alt="" />
        </nav>
    );
}

export default Nav;
